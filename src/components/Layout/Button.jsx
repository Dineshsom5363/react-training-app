import React from 'react'

const Button = ({title}) => {
  return (
    <div>
        <button className="bg-blue-500 text-white px-3 py-2 rounded">{title}</button>
    
    </div>
  )
}

export default Button